{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd demo/app && npm install && npm run build",
  "outputDirectory": "demo/app/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
